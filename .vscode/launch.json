{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Django",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/sellement/manage.py",
            "args": [
                "runserver"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}/sellement"
            },
            "django": true
        }
    ]
}